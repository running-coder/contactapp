(function (angular) {

    'use strict';

    angular
        .module('app.contacts')
        .controller('CreateContactController', CreateContactController);

    function CreateContactController($scope, ContactsService) {

    }
})(window.angular);
